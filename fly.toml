# fly.toml

app = "plantiq"
primary_region = "cdg"

[build]
  dockerfile = "scheduler/Dockerfile"

[processes]
  app = "python -m plantiq.run"

[schedule]
  cron = "0 7 * * *"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
